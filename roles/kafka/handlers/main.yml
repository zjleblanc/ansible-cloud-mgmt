---
- name: Start zookeeper
  ansible.builtin.systemd:
    name: zookeeper
    state: started
    enabled: true

- name: Start kafka
  ansible.builtin.systemd:
    name: kafka
    state: started
    enabled: true

- name: Start firewalld
  ansible.builtin.systemd:
    name: firewalld
    state: started
    enabled: true
