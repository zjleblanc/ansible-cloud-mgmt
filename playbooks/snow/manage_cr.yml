---
- name: Manage Change Requests
  hosts: "{{ _hosts }}"
  gather_facts: true

  tasks:
    - name: Complete CR Management
      ansible.builtin.include_tasks:
        file: "{{ manage_cr_action | default(action) }}_cr.yml"
...
