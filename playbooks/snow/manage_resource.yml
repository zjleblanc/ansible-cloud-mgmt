---
- name: Manage ServiceNow Resources
  hosts: "{{ _hosts }}"
  gather_facts: true

  tasks:
    - name: Complete CR Management
      ansible.builtin.include_tasks:
        file: "{{ sn_manage_action | default(action) }}_{{ sn_resource }}.yml"
...
